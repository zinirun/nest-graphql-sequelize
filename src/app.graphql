type Query {
    getUsers: [User]
    getUserById: User
}

type Mutation {
    createUser(user: UserInput): User
}

type User {
    id: Int!
    name: String!
    userId: String!
    password: String!
}

input UserInput {
    name: String!
    userId: String!
    password: String!
}
